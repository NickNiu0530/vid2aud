# 视频音频提取工具

![Python](https://img.shields.io/badge/Python-3.6%2B-blue)
![yt-dlp](https://img.shields.io/badge/yt--dlp-latest-green)
![FFmpeg](https://img.shields.io/badge/FFmpeg-required-orange)

一个用于从视频网站（如Bilibili、YouTube等）提取音频的Python脚本，使用yt-dlp库进行视频下载和音频提取。

## 📋 功能特点

- ✅ 支持从Bilibili、YouTube等多个视频网站提取音频
- ✅ 支持多种音频格式（mp3、wav、m4a等）
- ✅ 可自定义音频质量
- ✅ 支持单个视频和批量视频处理
- ✅ 自动检查和安装依赖项
- ✅ 友好的命令行界面和详细的进度显示
- ✅ 跨平台支持（Windows、macOS、Linux）

## 🚀 快速开始

### 1. 安装依赖

#### 必要依赖
- **Python 3.6+**：确保已安装Python 3.6或更高版本
- **FFmpeg**：用于音频提取和格式转换
- **yt-dlp**：用于视频下载

#### 安装FFmpeg

- **macOS**：
  ```bash
  brew install ffmpeg
  ```

- **Linux**：
  ```bash
  sudo apt-get install ffmpeg
  ```

- **Windows**（使用Chocolatey）：
  ```bash
  choco install ffmpeg
  ```

### 2. 安装脚本

1. 克隆或下载本仓库到本地
2. 进入脚本目录

### 3. 使用方法

#### 提取单个视频的音频

```bash
python3 extract_audio.py --url "视频URL" --output "输出目录" --format "音频格式" --quality "音频质量"
```

#### 批量提取多个视频的音频

1. 创建一个包含视频URL的文本文件（每行一个URL）
2. 运行命令：

```bash
python3 extract_audio.py --file "url文件路径" --output "输出目录" --format "音频格式" --quality "音频质量"
```

## 📝 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--url` | 单个视频的URL | 无 |
| `--file` | 包含视频URL列表的文件路径 | 无 |
| `--output` | 输出音频文件的目录 | `audio` |
| `--format` | 音频格式 | `mp3` |
| `--quality` | 音频质量 | `128k` |

## 🎯 示例

### 示例1：提取单个Bilibili视频的音频

```bash
python3 extract_audio.py --url "https://www.bilibili.com/video/BV1xx411c7mC" --output "bilibili_audio" --format "mp3" --quality "192k"
```

### 示例2：提取单个YouTube视频的音频

```bash
python3 extract_audio.py --url "https://www.youtube.com/watch?v=dQw4w9WgXcQ" --output "youtube_audio" --format "wav" --quality "320k"
```

### 示例3：批量提取多个视频的音频

1. 创建 `urls.txt` 文件：
   ```
   https://www.bilibili.com/video/BV1xx411c7mC
   https://www.bilibili.com/video/BV1Gx411z73W
   https://www.youtube.com/watch?v=dQw4w9WgXcQ
   ```

2. 运行命令：
   ```bash
   python3 extract_audio.py --file "urls.txt" --output "batch_audio" --format "mp3" --quality "128k"
   ```

## 🔧 技术细节

### 工作原理

1. **依赖检查**：脚本会自动检查FFmpeg和yt-dlp是否已安装
2. **视频下载**：使用yt-dlp下载视频的音频流
3. **音频提取**：使用FFmpeg从视频中提取音频
4. **格式转换**：将提取的音频转换为指定的格式和质量
5. **保存文件**：将处理后的音频保存到指定目录

### 支持的视频网站

- Bilibili
- YouTube
- Youku
- Tudou
- 爱奇艺
- 腾讯视频
- 优酷
- 以及其他yt-dlp支持的视频网站

## 📁 项目结构

```
.
├── extract_audio.py     # 主脚本文件
├── README.md            # 项目说明文档
└── .gitignore           # Git忽略文件
```

## 🔒 许可证

本项目采用MIT许可证。详见 [LICENSE](LICENSE) 文件。

## 🤝 贡献

欢迎提交Issue和Pull Request来帮助改进这个项目！

## 📞 联系方式

如果您有任何问题或建议，欢迎通过GitHub Issues与我联系。

---

**享受使用这个工具提取您喜爱的视频音频吧！** 🎵
